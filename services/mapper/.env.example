# PostgreSQL connection string
# When running via docker-compose, host = service name "postgres"
DATABASE_URL=postgresql://signalstock:signalstock_dev@localhost:5432/signalstock

# Seconds to sleep between polls when the event queue is empty (default: 30)
POLL_INTERVAL=30

# Max events to process per DB query (default: 10)
BATCH_SIZE=10
